// Example program
#include <iostream>
using namespace std;
#include <cmath>


int main()
{
 float a,b;
 a = 2;
 b = 13.13;
 float f;
 f = cbrt( exp(((1 - exp(a)) / b) * cos((b / a) * M_PI) + log(0.708 * b)) );
 cout << "\n" "expression f = " << f;
  
 float c,d;
 c = 2;
 d = 3.141;
float j;
j = sqrt(1 + (0.315 * log(c)) / sqrt((0.711 * log(d)) / (1 + sqrt(log(d / c)))));
cout<<"\n" "expression j = " << j;
}

